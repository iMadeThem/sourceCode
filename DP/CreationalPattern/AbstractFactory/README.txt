The main difference between AbstractFactory and Factory:
AbstractFactory provides interfaces for creating a set(including many serials) of related or depended objects.
Factory provides interfaces for a kind of objects or delay the creation of objects to the sub-class.
